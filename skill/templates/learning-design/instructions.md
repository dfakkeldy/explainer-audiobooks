# Learning-design starter records

Copy all six JSON files into a run's `research/` directory before canonical
drafting. Replace the public-safe email-classifier example with the approved
learner, outline, chapter, and concept evidence while the project develops.

These files are planning records, not receipts. The review starts pending and
has no chapter hashes. Only `learning_design_qc.py` may write the final
hash-bound learning receipt after both independent reviews pass.
