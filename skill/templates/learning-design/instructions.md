# Learning-design starter records

Copy all nine JSON files and the two Markdown starters into a run's `research/`
directory before pilot work. Build `evidence-notes.md` first and keep its claim
IDs synchronized with `evidence-notes.json`. Create a high-level
`voice-source-profile.md` from the approved brief or private source; do not copy
raw source excerpts into Git.
Replace the public-safe classifier example with the approved learner, road-book
or focused-study context, outline, chapter, concept, and pilot evidence while the
project develops.

These files are process records, not proof that learning occurred. The review
and comprehension pilot start pending. Full canonical drafting remains blocked
until the intended listener approves the argument-level outline, accepts the
first section as `voice-exemplar.md`, and accepts the 10-to-15-minute narrated
pilot. Draft the remainder section by section with explicit forward context.
Run every entry in `revision-passes.json` as a separate single-job pass. Only
`learning_design_qc.py` may write the final hash-bound learning receipt after
both independent reviews pass; later negative human listening evidence still
overrides that receipt.
